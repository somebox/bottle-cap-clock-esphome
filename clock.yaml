# ----------------------------------------------------------------------------
# Bottle Cap Clock - ESPHome configuration
#
# A 4-digit + colon clock built from 65 bottle-cap "pixels" wired as one
# WS2812 strip (3 LEDs per cap, 195 LEDs total) on a custom ESP32-S3 board.
#
# Hardware pinout (override with substitutions below if your wiring differs):
#   GPIO2   Heartbeat LED
#   GPIO6   User button (active low, internal pull-up)
#   GPIO8   I2C SDA (BH1750 ambient light sensor)
#   GPIO9   I2C SCL
#   GPIO16  Addressable LED data line (WS2812, 195 LEDs)
#
# This file follows the ESPHome "Made for ESPHome" / sharing guidelines so a
# kit owner can flash it from the dashboard and provision Wi-Fi without
# editing YAML. See README.md for the end-user setup flow.
# ----------------------------------------------------------------------------

substitutions:
  name: "bottle-cap-clock"
  friendly_name: "Bottle Cap Clock"
  # Project version - bump on every release so OTA updates show up correctly
  # in the ESPHome dashboard / Home Assistant.
  project_version: "1.0.0"
  # IANA tz database name. See:
  # https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  timezone: "Europe/Berlin"
  # Hardware pins. Override here if you re-wire the board.
  led_pin: GPIO16
  heartbeat_pin: GPIO2
  button_pin: GPIO6
  i2c_sda_pin: GPIO8
  i2c_scl_pin: GPIO9

esphome:
  name: "${name}"
  friendly_name: "${friendly_name}"
  # Append the chip MAC to the hostname so multiple kits coexist on a network
  # without renaming each one.
  name_add_mac_suffix: true
  # Project metadata. Required for ESPHome dashboard adoption.
  project:
    name: "bottlecapclock.esphome"
    version: "${project_version}"
  on_boot:
    priority: -100
    then:
      # Quick double blink of the heartbeat LED so you can see the firmware
      # actually started, even before Wi-Fi / network come up.
      - output.turn_on: heartbeat_led
      - delay: 200ms
      - output.turn_off: heartbeat_led
      - delay: 200ms
      - output.turn_on: heartbeat_led
      - delay: 200ms
      - output.turn_off: heartbeat_led
      # Force the clock effect on boot so the strip doesn't get stuck on a
      # diagnostic effect (Cap Walk / Digit Test) from a previous session.
      # Color and brightness come from the light's restored state.
      - light.turn_on:
          id: clock_leds
          effect: "Clock Display"

esp32:
  board: esp32-s3-devkitc-1
  variant: esp32s3
  flash_size: 16MB
  framework:
    type: arduino

# PSRAM is enabled for general use; the LED strip framebuffer is intentionally
# kept in SRAM (use_psram: false on the light) for deterministic DMA timing.
psram:
  mode: octal
  speed: 80MHz

logger:

# Native API for Home Assistant.
api:

# Over-the-air updates after the first USB flash.
ota:
  - platform: esphome
    id: ota_esphome

# Wi-Fi provisioning: device starts in AP mode if no credentials are saved.
# Connect a phone to the AP, the captive portal opens, and you enter your
# real Wi-Fi credentials. Improv (serial + BLE) provides the same flow from
# the ESPHome web tools or any Improv-compatible app.
#
# For local development you can hardcode credentials by replacing the AP
# block with:
#
#     ssid: !secret wifi_ssid
#     password: !secret wifi_password
#     ap:
#       ssid: "${friendly_name}"
#
# and creating secrets.yaml from secrets.yaml.example.
wifi:
  ap:
    ssid: "${friendly_name}"

captive_portal:

improv_serial:

esp32_improv:
  authorizer: none

# Dashboard adoption. Point package_import_url at the public raw URL of this
# file in your fork so users can adopt the device with one click from the
# ESPHome dashboard. Leave as-is for the upstream repo.
dashboard_import:
  package_import_url: github://bottlecapclock/bottle-cap-clock-esphome/clock.yaml@main
  import_full_config: false

# Web UI on http://<device-ip>/ for direct control without Home Assistant.
web_server:
  port: 80

i2c:
  id: bus_a
  sda: ${i2c_sda_pin}
  scl: ${i2c_scl_pin}
  scan: true

time:
  - platform: sntp
    id: sntp_time
    timezone: ${timezone}

# Heartbeat LED + 1 Hz blink so the user can tell the device is alive even
# when the clock display is intentionally dim or off.
output:
  - platform: gpio
    id: heartbeat_led
    pin: ${heartbeat_pin}

interval:
  - id: heartbeat_interval
    interval: 1s
    then:
      - output.turn_on: heartbeat_led
      - delay: 20ms
      - output.turn_off: heartbeat_led

binary_sensor:
  - platform: gpio
    name: "User Button"
    id: user_button
    pin:
      number: ${button_pin}
      mode: INPUT_PULLUP
      inverted: true

# BH1750 ambient light sensor. When "Auto Brightness" is on, on_value drives
# the clock brightness from a log-lux curve scaled by "Brightness Bias". A
# 3-sample median filter rejects single-sample spikes (passing shadows, brief
# reflections) without slowing the response.
sensor:
  - platform: bh1750
    name: "Ambient Light"
    id: ambient_light
    address: 0x23
    # Sample every 10 s, smooth across the last minute, and only publish a
    # new value every 30 s. Keeps auto-brightness reactions gentle and stops
    # the Ambient Light entity / Home Assistant history from churning on
    # every tiny lux fluctuation.
    update_interval: 10s
    filters:
      - median:
          window_size: 6
          send_every: 6
          send_first_at: 1
      - throttle: 30s
    # Pre-filter: cache the latest lux and apply auto-brightness on every
    # raw sample so reactions don't wait on the throttle/median used for
    # the published Ambient Light entity.
    on_raw_value:
      then:
        - lambda: 'id(last_lux) = x;'
        - script.execute: apply_auto_brightness

# Recomputes clock brightness from the cached last_lux + brightness_bias.
# Called whenever a fresh lux sample arrives, when the bias slider moves,
# or when auto-brightness is toggled on.
script:
  - id: apply_auto_brightness
    mode: restart
    then:
      - if:
          condition:
            lambda: 'return id(auto_brightness);'
          then:
            - lambda: |-
                // Log-lux to brightness, sized for the deployed PSU
                // (USB-only brownouts are a programming-time concern):
                //   1 lux -> 5%, 10 -> 29%, 100 -> 53%,
                //   1000 -> 77%, 10000+ -> 100%. Linear in log10(lux).
                float lux = id(last_lux);
                if (lux < 0.5f) lux = 0.5f;
                float log_lux = log10f(lux);
                float target = 0.05f + log_lux * 0.24f;
                if (target < 0.05f) target = 0.05f;
                if (target > 1.00f) target = 1.00f;
                target *= id(brightness_bias);
                if (target < 0.02f) target = 0.02f;
                if (target > 1.00f) target = 1.00f;
                auto call = id(clock_leds).turn_on();
                call.set_brightness(target);
                call.set_transition_length(2000);
                call.perform();

# Persistent runtime state. The select / switch / number entities below are
# the user-facing controls; these globals hold the cached values used by the
# per-frame display lambda.
globals:
  # These mirror the matching switch / select entities so the per-frame
  # display lambda can branch without going through the entity layer. The
  # entities own persistence (restore_mode / restore_value); the globals
  # are re-synced from them on boot via turn_on/turn_off_action.
  - id: rotate_180
    type: bool
    restore_value: false
    initial_value: 'false'
  - id: clock_mode
    type: int
    restore_value: false
    initial_value: '0'
  - id: auto_brightness
    type: bool
    restore_value: false
    initial_value: 'false'
  - id: brightness_bias
    type: float
    restore_value: false
    initial_value: '1.0'
  # Most recent raw lux from the BH1750. Updated on every sample (pre-filter)
  # so auto-brightness and the bias slider can react immediately without
  # waiting for the throttled, smoothed published value.
  - id: last_lux
    type: float
    restore_value: false
    initial_value: '50.0'

switch:
  - platform: template
    name: "Rotate 180"
    id: rotate_180_switch
    icon: "mdi:rotate-3d-variant"
    # The switch itself persists across reboots/reflashes (entity-id-keyed
    # preference, stable across firmware structure changes). On boot ESPHome
    # restores the saved state and replays turn_on/turn_off_action, which
    # re-syncs the rotate_180 global used by the display lambda.
    optimistic: true
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - globals.set:
          id: rotate_180
          value: 'true'
    turn_off_action:
      - globals.set:
          id: rotate_180
          value: 'false'
  - platform: template
    name: "Auto Brightness"
    id: auto_brightness_switch
    icon: "mdi:brightness-auto"
    optimistic: true
    restore_mode: RESTORE_DEFAULT_OFF
    turn_on_action:
      - globals.set:
          id: auto_brightness
          value: 'true'
      # Apply immediately using the cached last_lux instead of waiting for
      # the next (filtered, throttled) sensor publish.
      - script.execute: apply_auto_brightness
    turn_off_action:
      - globals.set:
          id: auto_brightness
          value: 'false'

number:
  - platform: template
    name: "Brightness Bias"
    id: brightness_bias_number
    icon: "mdi:tune-vertical"
    optimistic: true
    restore_value: true
    initial_value: 1.0
    min_value: 0.5
    max_value: 2.0
    step: 0.05
    mode: slider
    on_value:
      then:
        - globals.set:
            id: brightness_bias
            value: !lambda 'return x;'
        - script.execute: apply_auto_brightness

select:
  - platform: template
    name: "Clock Mode"
    id: clock_mode_select
    icon: "mdi:palette"
    optimistic: true
    restore_value: true
    options:
      - "Mono"
      - "Rainbow"
      - "Bicolor"
      - "Waves"
    initial_option: "Mono"
    on_value:
      then:
        - lambda: |-
            if      (x == "Mono")    id(clock_mode) = 0;
            else if (x == "Rainbow") id(clock_mode) = 1;
            else if (x == "Bicolor") id(clock_mode) = 2;
            else if (x == "Waves")   id(clock_mode) = 3;

# ----------------------------------------------------------------------------
# LED strip
#
# Layout along the data line (65 caps, 3 LEDs per cap, 195 LEDs total):
#   digit0 (15 caps)  digit1 (15)  colon (5)  digit2 (15)  digit3 (15)
#
# Each digit's caps are wired column-serpentine:
#   col 0 top -> bottom    (caps 0..4 within the digit)
#   col 1 bottom -> top    (caps 5..9)
#   col 2 top -> bottom    (caps 10..14)
# ROW_COL_TO_CAP encodes that mapping. See README for the visual diagram.
# ----------------------------------------------------------------------------
light:
  - platform: esp32_rmt_led_strip
    name: "Clock LEDs"
    id: clock_leds
    pin: ${led_pin}
    chipset: WS2812
    rgb_order: GRB
    num_leds: 195
    default_transition_length: 0s
    gamma_correct: 1.0
    # Persist the user's chosen color and brightness across reboots.
    restore_mode: RESTORE_DEFAULT_ON
    # ESP32-S3 timing: stream via DMA so Wi-Fi / scheduler interrupts can't
    # cause RMT buffer underruns mid-frame. rmt_symbols is the DMA chunk size
    # in bits; 1024 is the largest the new ESP-IDF RMT TX driver accepts here.
    # use_psram: false keeps the framebuffer in SRAM for deterministic access.
    use_dma: true
    rmt_symbols: 1024
    use_psram: false
    effects:
      # Diagnostic: walk one cap at a time through all 65 caps, 1.5s per step,
      # logging the cap index. Use this if you suspect a wiring or mapping
      # issue - it lets you read off the physical-to-logical cap order.
      - addressable_lambda:
          name: "Cap Walk"
          update_interval: 1500ms
          lambda: |-
            static const int LEDS_PER_CAP = 3;
            static const int NUM_LEDS = 195;
            static const int NUM_CAPS = 65;
            static int s_cap = 0;

            Color off = Color(0, 0, 0);
            Color on = Color(40, 40, 40);

            for (int i = 0; i < NUM_LEDS; i++) {
              it[i] = off;
            }
            int base = s_cap * LEDS_PER_CAP;
            it[base + 0] = on;
            it[base + 1] = on;
            it[base + 2] = on;

            ESP_LOGI("cap_walk", "cap=%d  leds=%d,%d,%d",
                     s_cap, base, base + 1, base + 2);
            s_cap = (s_cap + 1) % NUM_CAPS;

      # Diagnostic: cycle a single digit (0..9) on all four positions.
      # Useful for verifying the digit bitmap and (row, col) -> cap map
      # without depending on SNTP or color modes.
      - addressable_lambda:
          name: "Digit Test"
          update_interval: 1000ms
          lambda: |-
            static const uint8_t ROW_COL_TO_CAP[5][3] = {
                {0, 9, 10},
                {1, 8, 11},
                {2, 7, 12},
                {3, 6, 13},
                {4, 5, 14},
            };
            static const char *const DIGITS[10][5] = {
                {"111", "101", "101", "101", "111"},  // 0
                {"001", "001", "001", "001", "001"},  // 1
                {"111", "001", "111", "100", "111"},  // 2
                {"111", "001", "111", "001", "111"},  // 3
                {"101", "101", "111", "001", "001"},  // 4
                {"111", "100", "111", "001", "111"},  // 5
                {"111", "100", "111", "101", "111"},  // 6
                {"111", "001", "001", "001", "001"},  // 7
                {"111", "101", "111", "101", "111"},  // 8
                {"111", "101", "111", "001", "111"},  // 9
            };
            static const int DIGIT_OFFSET[4] = {0, 15, 35, 50};
            static const int LEDS_PER_CAP = 3;

            static int s_value = 0;
            int n = s_value;
            s_value = (s_value + 1) % 10;

            it.all() = Color(0, 0, 0);
            for (int d = 0; d < 4; d++) {
              int offset = DIGIT_OFFSET[d];
              for (int row = 0; row < 5; row++) {
                for (int col = 0; col < 3; col++) {
                  if (DIGITS[n][row][col] == '1') {
                    int cap = offset + ROW_COL_TO_CAP[row][col];
                    int base = cap * LEDS_PER_CAP;
                    it[base + 0] = current_color;
                    it[base + 1] = current_color;
                    it[base + 2] = current_color;
                  }
                }
              }
            }

      # Normal clock display: HH:MM driven by SNTP, colon fades at 0.5 Hz.
      # Caches the last valid time so a brief SNTP desync doesn't blank the
      # display. Coloring depends on the active Clock Mode (see select).
      - addressable_lambda:
          name: "Clock Display"
          update_interval: 50ms
          lambda: |-
            static const uint8_t ROW_COL_TO_CAP[5][3] = {
                {0, 9, 10},
                {1, 8, 11},
                {2, 7, 12},
                {3, 6, 13},
                {4, 5, 14},
            };
            static const char *const DIGITS[10][5] = {
                {"111", "101", "101", "101", "111"},  // 0
                {"001", "001", "001", "001", "001"},  // 1
                {"111", "001", "111", "100", "111"},  // 2
                {"111", "001", "111", "001", "111"},  // 3
                {"101", "101", "111", "001", "001"},  // 4
                {"111", "100", "111", "001", "111"},  // 5
                {"111", "100", "111", "101", "111"},  // 6
                {"111", "001", "001", "001", "001"},  // 7
                {"111", "101", "111", "101", "111"},  // 8
                {"111", "101", "111", "001", "111"},  // 9
            };
            static const int DIGIT_OFFSET[4] = {0, 15, 35, 50};
            static const int COLON_OFFSET = 30;
            static const int LEDS_PER_CAP = 3;

            // Visible left-to-right column for spatial modes:
            //   digit 0 -> 0..2   digit 1 -> 3..5   colon -> 6
            //   digit 2 -> 7..9   digit 3 -> 10..12   width = 13
            static const int DIGIT_X_OFFSET[4] = {0, 3, 7, 10};
            static const int COLON_X = 6;
            static const float DISPLAY_W = 13.0f;

            static int s_hour = 0;
            static int s_minute = 0;
            static bool s_have_time = false;

            auto now = id(sntp_time).now();
            if (now.is_valid()) {
              s_hour = now.hour;
              s_minute = now.minute;
              s_have_time = true;
            }

            it.all() = Color(0, 0, 0);
            if (!s_have_time) {
              return;
            }

            int digits[4] = {
              s_hour / 10,
              s_hour % 10,
              s_minute / 10,
              s_minute % 10,
            };

            auto paint_cap = [&](int cap, Color color) {
              int base = cap * LEDS_PER_CAP;
              it[base + 0] = color;
              it[base + 1] = color;
              it[base + 2] = color;
            };

            int mode = id(clock_mode);
            float t_ms = (float)millis();

            // Rainbow uses raw user brightness because current_color already
            // bakes brightness in; double-applying would over-dim.
            float user_brightness =
                id(clock_leds).current_values.get_brightness();

            auto hsv_to_color = [](float h, float s, float v) -> Color {
              h = h - floorf(h);
              float r = 0, g = 0, b = 0;
              int i = (int)(h * 6.0f);
              float f = h * 6.0f - i;
              float p = v * (1.0f - s);
              float q = v * (1.0f - f * s);
              float u = v * (1.0f - (1.0f - f) * s);
              switch (i % 6) {
                case 0: r = v; g = u; b = p; break;
                case 1: r = q; g = v; b = p; break;
                case 2: r = p; g = v; b = u; break;
                case 3: r = p; g = q; b = v; break;
                case 4: r = u; g = p; b = v; break;
                case 5: r = v; g = p; b = q; break;
              }
              return Color((uint8_t)(r * 255.0f),
                           (uint8_t)(g * 255.0f),
                           (uint8_t)(b * 255.0f));
            };

            auto scale_color = [](Color c, float k) -> Color {
              if (k < 0.0f) k = 0.0f;
              if (k > 1.0f) k = 1.0f;
              return Color((uint8_t)(c.r * k),
                           (uint8_t)(c.g * k),
                           (uint8_t)(c.b * k));
            };

            // Bicolor secondary = primary with channels rotated R<-G<-B<-R.
            // Brightness-preserving and visibly distinct from the primary.
            Color minutes_color = Color(current_color.g,
                                         current_color.b,
                                         current_color.r);

            auto color_for = [&](int x, int logical_d) -> Color {
              switch (mode) {
                case 1: { // Rainbow: hue = position + 30s drift
                  float h = ((float)x / DISPLAY_W) - (t_ms / 30000.0f);
                  return scale_color(hsv_to_color(h, 1.0f, 1.0f),
                                     user_brightness);
                }
                case 2: { // Bicolor: hours = primary, minutes = rotated.
                  // Colon (logical_d == -1) alternates each blink so the
                  // dots cycle between the two colors over time.
                  if (logical_d == -1) {
                    int blink = (int)(t_ms / 2000.0f);
                    return (blink & 1) ? minutes_color : current_color;
                  }
                  if (logical_d == 2 || logical_d == 3) return minutes_color;
                  return current_color;
                }
                case 3: { // Waves: visible brightness sweep + gentle hue drift.
                  // Brightness floor is 0.5 so digits with few lit caps
                  // (notably "1") stay readable through the dip. Range
                  // 0.5..1.0.
                  float w = 0.75f + 0.25f * sinf((float)x * 0.6f -
                                                 t_ms / 1200.0f);
                  // Color shift: cross-blend up to 20% with a channel-
                  // rotated copy of the user color. Subtle hue motion that
                  // stays anchored to the chosen color.
                  float c = 0.10f + 0.10f * sinf((float)x * 0.4f -
                                                 t_ms / 2500.0f);
                  Color alt = Color(current_color.b,
                                    current_color.r,
                                    current_color.g);
                  uint8_t r = (uint8_t)(current_color.r * (1.0f - c) +
                                        alt.r * c);
                  uint8_t g = (uint8_t)(current_color.g * (1.0f - c) +
                                        alt.g * c);
                  uint8_t b = (uint8_t)(current_color.b * (1.0f - c) +
                                        alt.b * c);
                  return scale_color(Color(r, g, b), w);
                }
                default:  // Mono
                  return current_color;
              }
            };

            // 180-degree rotation: swap digit order and flip (row, col).
            // Mode coloring uses logical/visible coordinates so gradients
            // always read correctly regardless of rotation.
            bool rotate = id(rotate_180);
            for (int d = 0; d < 4; d++) {
              int phys_d = rotate ? (3 - d) : d;
              int offset = DIGIT_OFFSET[phys_d];
              int n = digits[d];
              for (int row = 0; row < 5; row++) {
                for (int col = 0; col < 3; col++) {
                  if (DIGITS[n][row][col] == '1') {
                    int phys_row = rotate ? (4 - row) : row;
                    int phys_col = rotate ? (2 - col) : col;
                    int x = DIGIT_X_OFFSET[d] + col;
                    paint_cap(offset + ROW_COL_TO_CAP[phys_row][phys_col],
                              color_for(x, d));
                  }
                }
              }
            }

            // Colon: light caps 1 and 3 of the 5-cap column with a 0.5 Hz
            // (2 s period) cosine fade in the active mode's color.
            float phase = (millis() % 2000) / 2000.0f;
            float fade = 0.5f * (1.0f - cosf(phase * 2.0f * 3.14159265f));
            Color colon_base = color_for(COLON_X, -1);
            Color colon_color = scale_color(colon_base, fade);
            paint_cap(COLON_OFFSET + 1, colon_color);
            paint_cap(COLON_OFFSET + 3, colon_color);
