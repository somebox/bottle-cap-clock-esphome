# Optional. Only needed if you switch the wifi: block in clock.yaml to use
# !secret references instead of the default captive-portal / Improv flow.
# Copy this file to secrets.yaml and fill in your credentials.
wifi_ssid: "YourNetworkName"
wifi_password: "YourPassword"
